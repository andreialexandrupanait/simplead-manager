; Deliberately shared-hosting-like limits so the chunking / resume paths
; actually engage during the spike (a spike that never hits a limit proves nothing).
memory_limit = 128M
max_execution_time = 30
max_input_time = 30
post_max_size = 64M
upload_max_filesize = 64M
date.timezone = UTC
